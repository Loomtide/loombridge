# --- Node / TypeScript ---
node_modules/
dist/
*.tgz
*.log

# Generated at prepack (never committed)
mcp-server/bridge/
mcp-server/agent-surface/

# Trace recorder output. Left untracked this made `git status --porcelain` dirty
# after any test run, which made write-build-info stamp every released artifact
# "+dirty" — the provenance stamp could never distinguish a real dirty build.
trace/

# --- Unity ---
Library/
Logs/
UserSettings/
Temp/
obj/
Build/
Builds/
MemoryCaptures/
crashlytics-build.properties

# --- Loombridge per-project state ---
.loombridge/
!templates/create-loombridge-game/.loombridge/

# --- OS ---
.DS_Store
Thumbs.db
